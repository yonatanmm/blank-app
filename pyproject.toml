[project]
name = "blank-app-template"
version = "0.1.0"
description = "A simple Streamlit app template."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "streamlit>=1.61.1",
    "numpy",
    "pandas",
    "requests",
    "python-dotenv",
    "openai",
    "plotly",
    "openpyxl",
    "xlrd",
]

[tool.uv]
package = false
